# uart_project

串口回声：敲什么回什么。第一次让板子跟电脑"说上话"。

- 乱码第一反应查代码，其实是串口助手波特率没对上（115200, 8N1），双方参数必须一致。
- `HAL_UART_Receive` 是阻塞的，等在那不返回——这也算第一次直观感受到"阻塞"是什么意思。
- PA2 要配 `GPIO_MODE_AF_PP` 复用推挽，不是普通输出。

接线：PA2→USB转串口 RX，PA3→TX。
