# adc_project

电位器接 PA0，电压越高 LED 闪越快。

- 换算：`电压mV = 读数 × 3300 / 4095`。12 位就是 0~4095，除以 4095 不是 4096，被这个小数点较过真。
- 采样流程四步：Start → PollForConversion → GetValue → Stop，顺序反了或漏 Stop 都会出怪事。
- 采样时间拉长到 239.5 周期后读数明显变稳，便宜板子电源噪声还是有的。
