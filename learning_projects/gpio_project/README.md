# gpio_project

2026-07 重的第一个工程。就是点灯，但把"开时钟 → 配引脚 → 写电平"这个套路固定下来了，后面所有工程都是这么起的头。

- PC13 低电平点亮，`GPIO_PIN_SET` 反而是灭，一开始记反了还以为板子坏了。
- 忘开 `__HAL_RCC_GPIOC_CLK_ENABLE()` 的话写寄存器完全没反应，这个坑踩过一次就记住了。

现象：板载 LED 500ms 闪。没了。
