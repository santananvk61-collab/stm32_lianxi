# STM32 项目

这是一个 STM32 嵌入式项目（基于 STM32F10x，使用 Keil MDK / 标准外设库）。

## 目录结构

（待补充）

## 说明

- 源码文件统一使用 UTF-8 编码，避免中文在终端/编辑器里乱码。
- 编译和烧录产生的临时文件已被 `.gitignore` 忽略，不会提交进仓库。
- 上手流程：`git clone` 仓库 → 用 Keil / STM32CubeIDE 打开工程 → 编译烧录。
